apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sneakers.fullname" . }}-test-connection"
  labels:
    {{- include "sneakers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sneakers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
